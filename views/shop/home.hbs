<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop Home Page</title>
    <style>
        .product-list{
            display: flex;
            justify-content: space-around;
            width: 100%;
            margin-top: 30px;
        }
        .product-item{
            padding: 15px;
            margin-bottom: 15px;
            box-shadow:  5px 10px 10px goldenrod;
            border: 2px solid rgb(250, 250, 137);
            text-align: center;
        }
        .product-item .product-image{
            width: 230px;
            height: 220px;
            border: 2px solid black;
            border-radius: 5%;
            margin-bottom: 5px;
        }
        .category {
            width: 100%;
        }
        .key {
            border-bottom: 2px solid yellow;
            border-top: 2px solid yellow;
            margin: 25px 15px;
            font-size: 25px;
            text-align: center;

        }
    
    </style>
</head>
<link rel="stylesheet" href="/CSS/navbar.css">
<link rel="stylesheet" href="/CSS/product-list-button.css">
<body>
    
    {{>navbar}}
    <h1>Welcome to BB Shop</h1>

    <div class="category">
        {{#each products as |category| }}
            <div class="key">{{@key}}</div>
            <div class="product-list">
                {{#each category as |p| }}
                    <div class="product-item">
                        <img class="product-image" src={{p.imageUrl}}> <br>
                        <h3> Product name : {{p.Name}}</h3>
                        <h3> Product price: {{p.Price}}</h3>
                        {{!-- <h3> Product description : {{p.Description}}</h3>
                        <h3> Product seller : {{p.Seller}}</h3> --}}
                        <div class="button-group">
                            <a href="/shop/products/{{p._id}}">
                                <button> View🔍 </button>
                            </a>
                            <a href="/shop/cart/add/{{p._id}}">
                                <button> Add to Cart🛒 </button>
                            </a>
                        </div>
                        
                    </div>
                {{/each }}
            </div>
        {{/each }}
    </div>


</body>
</html>